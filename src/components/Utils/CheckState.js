/**
 * Return yes or no in tems of a boolean value
 * @param {boolean} bool State of a element
 * @returns {string} [true]->yes / [false]->no
 */
function CheckState(bool) {
   return bool ? "yes" : "no" ;
}

export default CheckState